# Database (Supabase Postgres or Prisma Accelerate)
# For Supabase: postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
# Encode special characters in password (e.g. # -> %23)
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.YOUR_PROJECT_REF.supabase.co:5432/postgres

# Supabase (Storage / optional Auth)
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_KEY=your_anon_or_service_role_key
SUPABASE_AVATAR_BUCKET_NAME=avatars

# Server
HOST=http://localhost:3001
BASE=/api/v1
PORT=3001
NODE_ENV=development

# Auth (JWT, Google OAuth, etc.)
JWT_SECRET=your_jwt_secret_min_32_chars
FRONTEND_URL=http://localhost:5173
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Dev / testing (userController requires these when NODE_ENV !== production)
TEST_EMAIL=test@example.com

# Optional: Redis, Gmail for emails, etc.
# REDIS_URL=redis://localhost:6379
# GMAIL_ACCOUNT=
# GMAIL_APP_PASSWORD=
